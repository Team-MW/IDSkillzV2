import React from 'react';

const CTA = () => {
    return (
        <section className="cta">
            <div className="container">
                <h2 className="cta-title">Prêt à passer à l'action ?</h2>
                <a href="#contact" className="btn-primary">Contactez-nous</a>
            </div>
        </section>
    );
};

export default CTA;
